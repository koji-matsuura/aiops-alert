# main.yaml with all nested stacks
AWSTemplateFormatVersion: '2010-09-09'
Description: AIOps Bedrock Agent CloudFormation
Parameters:
  TemplateBucketName:
    Type: String
    Description: S3 bucket where CloudFormation templates are stored
  EnvName:
    Type: String
    Description: Environment name (dev, stg, prd)
    Default: dev
  FoundationModel:
    Type: String
    Default: anthropic.claude-haiku-4-5-20251001-v1:0
    Description: Bedrock Agent用ファンデーションモデル
  VectorIndexName:
    Type: String
    Default: aiops-kb-index
    Description: OpenSearch内のベクトルインデックス名
  EmbeddingModelArn:
    Type: String
    Default: arn:aws:bedrock:ap-northeast-1::foundation-model/amazon.titan-embed-text-v2:0
    Description: Bedrock TitanエンベーディングモデルのARN
  SlackWorkspaceId:
    Type: String
    Description: Slack Workspace ID
    Default: ""
  SlackChannelId:
    Type: String
    Description: Slack Channel ID for notifications
    Default: ""
  ServiceName:
    Type: String
    Description: Service name for resource naming
    Default: aiops
Resources:
  S3Stack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/s3.yaml'
      Parameters:
        EnvName: !Ref EnvName
        ThreadMappingRetentionDays: "1"
        PendingConfirmationRetentionDays: "7"
        ReportsRetentionDays: "30"

  OpensearchStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/opensearch.yaml'

  LambdaStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/lambda-function.yaml'
      Parameters:
        BucketName: !Ref TemplateBucketName
        SNSTopicArn: !Sub 'arn:aws:sns:${AWS::Region}:${AWS::AccountId}:${EnvName}-aiops-notification'
        BedrockAgentId: ''
        BedrockAgentAlias: 'TSTALIASID'

  SecretsManagerStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/secrets-manager.yaml'
      Parameters:
        EnvironmentName: !Ref EnvName

  SlackWebhookStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/slack-webhook.yaml'
      Parameters:
        TemplateBucketName: !Ref TemplateBucketName
        EnvironmentName: !Ref EnvName
        S3DataBucket: !GetAtt S3Stack.Outputs.BucketName
        SlackCredentialsSecretArn: !GetAtt SecretsManagerStack.Outputs.SlackCredentialsSecretArn

  ChatbotSlackNotificationStack:
    Type: AWS::CloudFormation::Stack
    DependsOn: SlackWebhookStack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/chatbot-slack-notification.yaml'
      Parameters:
        EnvName: !Ref EnvName
        ServiceName: !Ref ServiceName
        SlackWorkspaceId: !Ref SlackWorkspaceId
        SlackChannelId: !Ref SlackChannelId

  KnowledgeBaseStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/knowledge-base.yaml'
      Parameters:
        OpenSearchCollectionArn: !GetAtt OpensearchStack.Outputs.CollectionArn
        S3DataBucketArn: !GetAtt S3Stack.Outputs.BucketArn
        VectorIndexName: !Ref VectorIndexName
        EmbeddingModelArn: !Ref EmbeddingModelArn

  BedrockAgentStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/bedrock-agent.yaml'
      Parameters:
        FoundationModel: !Ref FoundationModel
        KnowledgeBaseId: !GetAtt KnowledgeBaseStack.Outputs.KnowledgeBaseId
        ActionGroupLambdaArn: !GetAtt LambdaStack.Outputs.LambdaARN

  # Update Lambda Environment with Bedrock Agent ID after Agent is created
  LambdaUpdateStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/lambda-function.yaml'
      Parameters:
        BucketName: !Ref TemplateBucketName
        SNSTopicArn: !Sub 'arn:aws:sns:${AWS::Region}:${AWS::AccountId}:${EnvName}-aiops-notification'
        BedrockAgentId: !GetAtt BedrockAgentStack.Outputs.AgentId
        BedrockAgentAlias: 'TSTALIASID'

  EventBridgeAlarmsStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub 'https://s3.${AWS::Region}.amazonaws.com/${TemplateBucketName}/cfn-templates/eventbridge-alarms.yaml'
      Parameters:
        LambdaFunctionArn: !GetAtt LambdaStack.Outputs.LambdaARN
        Environment: !Ref EnvName

Outputs: {}
